services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:./prisma/prod.db
      - NODE_ENV=production
    volumes:
      - db-data:/app/prisma
    restart: unless-stopped

volumes:
  db-data:
